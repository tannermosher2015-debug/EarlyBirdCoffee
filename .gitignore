# OS / editor junk
.DS_Store
Thumbs.db
desktop.ini
*.swp
.vscode/
.idea/

# Netlify local
.netlify/

# Internal agent notes — keep local, don't publish (repo root is the Netlify publish dir)
CLAUDE.md
